import FormPanel from "../../components/FormPanel";

import SignUpForm from "./SignUpForm";

const SignUp = () => {
  return (
    <FormPanel>
      <SignUpForm />
    </FormPanel>
  );
};

export default SignUp;
